[pytest]
markers =
    sdk: client SDK unit and regression tests
    integration: end-to-end tests against a running API
    server: server/internal tests

#$env:E2E_BASE_URL="http://localhost:8000"
#pytest tests\integration -v